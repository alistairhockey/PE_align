/*
 * Site credentials — TEMPLATE.
 * ---------------------------
 * Copy to conf/secrets.config and fill in. conf/secrets.config is git-ignored
 * and must never be committed.
 *
 *   cp conf/secrets.config.template conf/secrets.config
 *   chmod 600 conf/secrets.config
 *   nextflow run . -c conf/secrets.config ...
 *
 * Prefer environment variables over a file wherever the site allows it:
 *   export AWS_ACCESS_KEY_ID=...
 *   export AWS_SECRET_ACCESS_KEY=...
 *
 * If credentials have ever been committed, shared, or left readable on a
 * multi-user system, rotate them. Removing them from a later commit does not
 * remove them from git history.
 */

// Pawsey Acacia (S3-compatible object storage)
// aws {
//     accessKey = System.getenv('AWS_ACCESS_KEY_ID')
//     secretKey = System.getenv('AWS_SECRET_ACCESS_KEY')
//     client {
//         endpoint          = 'https://acacia.pawsey.org.au'
//         s3PathStyleAccess = true
//     }
// }

// Pawsey project code for SLURM accounting
// params.setonix_account = 'pawseyXXXX'
